import 'package:flutter/material.dart';

class EatHealthy extends StatelessWidget {
  const EatHealthy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}